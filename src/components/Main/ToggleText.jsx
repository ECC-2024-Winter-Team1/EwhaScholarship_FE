import styled from "styled-components";

const StyledText = styled.p`
  font-size: 20px;
  margin-left: 225px;
`;

export default function ToggleText() {
  return <StyledText>내 정보로 바로 검색하기</StyledText>;
}
